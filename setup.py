from setuptools import setup

requirements = [
      'scikit-learn',
      'matplotlib',
      'six',
      'numpy',
      'scipy',
      'tensorflow',
      'pandas']

setup(name='tablegan',
      version='1.0',
      install_requires=requirements
      )
